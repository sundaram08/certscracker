/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
         "cdn.pixabay.com",
         "s3-alpha-sig.figma.com",
         "www.freepik.com"
        ],
      },
};

export default nextConfig;
